name: CI
on:
  push:
    branches: [ extended ]
  pull_request:
    branches: [ extended ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: appclearn
        run: |
          .buildozer/buildozer appclean
a