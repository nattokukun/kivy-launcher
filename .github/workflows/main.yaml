name: CI
on:
  push:
    branches: [ extended ]
  pull_request:
    branches: [ extended ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: appclearn
##           path: .buildozer
       run: |
          .buildozer/buildozer appclean
a